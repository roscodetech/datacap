import 'package:flutter/material.dart';

Widget buildCameraPreview(String viewId) {
  return const Center(
    child: Text(
      'Camera preview not available on this platform',
      style: TextStyle(color: Colors.white),
    ),
  );
}
